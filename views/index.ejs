<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <h1>Mini Message Board</h1>
    <ul>
        <% messages.forEach(message=> { %>
            <li><strong>
                    <%= message.user %>:
                </strong>
                <%= message.text %> <em>(<%= message.added ? message.added.toLocaleString() : 'Unknown date' %>)</em>
            </li>
            <% }); %>
    </ul>
    <a href="/new" class="action-btn">Add a New Message</a>
</body>

</html>